repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.8
    hooks:
      # Run ruff linter with auto-fix (matches `uv run ruff check --fix .`)
      - id: ruff
        args: [--fix]
      # Run ruff formatter (matches `uv run ruff format .`)
      - id: ruff-format
